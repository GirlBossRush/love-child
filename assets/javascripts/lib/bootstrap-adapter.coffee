# Bootstrap's plugins are jQuery heavy and depend on more traditional loading schemes.
# Requiring the modules will activate the jQuery bindings.
modal = require("bootstrap/js/modal")

module.exports = {modal}
